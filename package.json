{
  "name": "mahni-pleyer",
  "version": "1.0.0",
  "description": "Azerbaijani Music Player with native Android permissions",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Static project - no build step'",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/filesystem": "^6.0.0",
    "capacitor-file-picker": "^1.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
